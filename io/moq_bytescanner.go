// Code generated by Moqueries - https://moqueries.org - DO NOT EDIT!

package io

import (
	"fmt"
	"io"
	"math/bits"
	"sync/atomic"

	"moqueries.org/runtime/moq"
)

// The following type assertion assures that io.ByteScanner is mocked
// completely
var _ io.ByteScanner = (*MoqByteScanner_mock)(nil)

// MoqByteScanner holds the state of a moq of the ByteScanner type
type MoqByteScanner struct {
	Scene  *moq.Scene
	Config moq.Config
	Moq    *MoqByteScanner_mock

	ResultsByParams_ReadByte   []MoqByteScanner_ReadByte_resultsByParams
	ResultsByParams_UnreadByte []MoqByteScanner_UnreadByte_resultsByParams

	Runtime struct {
		ParameterIndexing struct {
			ReadByte   struct{}
			UnreadByte struct{}
		}
	}
}

// MoqByteScanner_mock isolates the mock interface of the ByteScanner type
type MoqByteScanner_mock struct {
	Moq *MoqByteScanner
}

// MoqByteScanner_recorder isolates the recorder interface of the ByteScanner
// type
type MoqByteScanner_recorder struct {
	Moq *MoqByteScanner
}

// MoqByteScanner_ReadByte_params holds the params of the ByteScanner type
type MoqByteScanner_ReadByte_params struct{}

// MoqByteScanner_ReadByte_paramsKey holds the map key params of the
// ByteScanner type
type MoqByteScanner_ReadByte_paramsKey struct {
	Params struct{}
	Hashes struct{}
}

// MoqByteScanner_ReadByte_resultsByParams contains the results for a given set
// of parameters for the ByteScanner type
type MoqByteScanner_ReadByte_resultsByParams struct {
	AnyCount  int
	AnyParams uint64
	Results   map[MoqByteScanner_ReadByte_paramsKey]*MoqByteScanner_ReadByte_results
}

// MoqByteScanner_ReadByte_doFn defines the type of function needed when
// calling AndDo for the ByteScanner type
type MoqByteScanner_ReadByte_doFn func()

// MoqByteScanner_ReadByte_doReturnFn defines the type of function needed when
// calling DoReturnResults for the ByteScanner type
type MoqByteScanner_ReadByte_doReturnFn func() (byte, error)

// MoqByteScanner_ReadByte_results holds the results of the ByteScanner type
type MoqByteScanner_ReadByte_results struct {
	Params  MoqByteScanner_ReadByte_params
	Results []struct {
		Values *struct {
			Result1 byte
			Result2 error
		}
		Sequence   uint32
		DoFn       MoqByteScanner_ReadByte_doFn
		DoReturnFn MoqByteScanner_ReadByte_doReturnFn
	}
	Index  uint32
	Repeat *moq.RepeatVal
}

// MoqByteScanner_ReadByte_fnRecorder routes recorded function calls to the
// MoqByteScanner moq
type MoqByteScanner_ReadByte_fnRecorder struct {
	Params    MoqByteScanner_ReadByte_params
	AnyParams uint64
	Sequence  bool
	Results   *MoqByteScanner_ReadByte_results
	Moq       *MoqByteScanner
}

// MoqByteScanner_ReadByte_anyParams isolates the any params functions of the
// ByteScanner type
type MoqByteScanner_ReadByte_anyParams struct {
	Recorder *MoqByteScanner_ReadByte_fnRecorder
}

// MoqByteScanner_UnreadByte_params holds the params of the ByteScanner type
type MoqByteScanner_UnreadByte_params struct{}

// MoqByteScanner_UnreadByte_paramsKey holds the map key params of the
// ByteScanner type
type MoqByteScanner_UnreadByte_paramsKey struct {
	Params struct{}
	Hashes struct{}
}

// MoqByteScanner_UnreadByte_resultsByParams contains the results for a given
// set of parameters for the ByteScanner type
type MoqByteScanner_UnreadByte_resultsByParams struct {
	AnyCount  int
	AnyParams uint64
	Results   map[MoqByteScanner_UnreadByte_paramsKey]*MoqByteScanner_UnreadByte_results
}

// MoqByteScanner_UnreadByte_doFn defines the type of function needed when
// calling AndDo for the ByteScanner type
type MoqByteScanner_UnreadByte_doFn func()

// MoqByteScanner_UnreadByte_doReturnFn defines the type of function needed
// when calling DoReturnResults for the ByteScanner type
type MoqByteScanner_UnreadByte_doReturnFn func() error

// MoqByteScanner_UnreadByte_results holds the results of the ByteScanner type
type MoqByteScanner_UnreadByte_results struct {
	Params  MoqByteScanner_UnreadByte_params
	Results []struct {
		Values *struct {
			Result1 error
		}
		Sequence   uint32
		DoFn       MoqByteScanner_UnreadByte_doFn
		DoReturnFn MoqByteScanner_UnreadByte_doReturnFn
	}
	Index  uint32
	Repeat *moq.RepeatVal
}

// MoqByteScanner_UnreadByte_fnRecorder routes recorded function calls to the
// MoqByteScanner moq
type MoqByteScanner_UnreadByte_fnRecorder struct {
	Params    MoqByteScanner_UnreadByte_params
	AnyParams uint64
	Sequence  bool
	Results   *MoqByteScanner_UnreadByte_results
	Moq       *MoqByteScanner
}

// MoqByteScanner_UnreadByte_anyParams isolates the any params functions of the
// ByteScanner type
type MoqByteScanner_UnreadByte_anyParams struct {
	Recorder *MoqByteScanner_UnreadByte_fnRecorder
}

// NewMoqByteScanner creates a new moq of the ByteScanner type
func NewMoqByteScanner(scene *moq.Scene, config *moq.Config) *MoqByteScanner {
	if config == nil {
		config = &moq.Config{}
	}
	m := &MoqByteScanner{
		Scene:  scene,
		Config: *config,
		Moq:    &MoqByteScanner_mock{},

		Runtime: struct {
			ParameterIndexing struct {
				ReadByte   struct{}
				UnreadByte struct{}
			}
		}{ParameterIndexing: struct {
			ReadByte   struct{}
			UnreadByte struct{}
		}{
			ReadByte:   struct{}{},
			UnreadByte: struct{}{},
		}},
	}
	m.Moq.Moq = m

	scene.AddMoq(m)
	return m
}

// Mock returns the mock implementation of the ByteScanner type
func (m *MoqByteScanner) Mock() *MoqByteScanner_mock { return m.Moq }

func (m *MoqByteScanner_mock) ReadByte() (result1 byte, result2 error) {
	m.Moq.Scene.T.Helper()
	params := MoqByteScanner_ReadByte_params{}
	var results *MoqByteScanner_ReadByte_results
	for _, resultsByParams := range m.Moq.ResultsByParams_ReadByte {
		paramsKey := m.Moq.ParamsKey_ReadByte(params, resultsByParams.AnyParams)
		var ok bool
		results, ok = resultsByParams.Results[paramsKey]
		if ok {
			break
		}
	}
	if results == nil {
		if m.Moq.Config.Expectation == moq.Strict {
			m.Moq.Scene.T.Fatalf("Unexpected call to %s", m.Moq.PrettyParams_ReadByte(params))
		}
		return
	}

	i := int(atomic.AddUint32(&results.Index, 1)) - 1
	if i >= results.Repeat.ResultCount {
		if !results.Repeat.AnyTimes {
			if m.Moq.Config.Expectation == moq.Strict {
				m.Moq.Scene.T.Fatalf("Too many calls to %s", m.Moq.PrettyParams_ReadByte(params))
			}
			return
		}
		i = results.Repeat.ResultCount - 1
	}

	result := results.Results[i]
	if result.Sequence != 0 {
		sequence := m.Moq.Scene.NextMockSequence()
		if (!results.Repeat.AnyTimes && result.Sequence != sequence) || result.Sequence > sequence {
			m.Moq.Scene.T.Fatalf("Call sequence does not match call to %s", m.Moq.PrettyParams_ReadByte(params))
		}
	}

	if result.DoFn != nil {
		result.DoFn()
	}

	if result.Values != nil {
		result1 = result.Values.Result1
		result2 = result.Values.Result2
	}
	if result.DoReturnFn != nil {
		result1, result2 = result.DoReturnFn()
	}
	return
}

func (m *MoqByteScanner_mock) UnreadByte() (result1 error) {
	m.Moq.Scene.T.Helper()
	params := MoqByteScanner_UnreadByte_params{}
	var results *MoqByteScanner_UnreadByte_results
	for _, resultsByParams := range m.Moq.ResultsByParams_UnreadByte {
		paramsKey := m.Moq.ParamsKey_UnreadByte(params, resultsByParams.AnyParams)
		var ok bool
		results, ok = resultsByParams.Results[paramsKey]
		if ok {
			break
		}
	}
	if results == nil {
		if m.Moq.Config.Expectation == moq.Strict {
			m.Moq.Scene.T.Fatalf("Unexpected call to %s", m.Moq.PrettyParams_UnreadByte(params))
		}
		return
	}

	i := int(atomic.AddUint32(&results.Index, 1)) - 1
	if i >= results.Repeat.ResultCount {
		if !results.Repeat.AnyTimes {
			if m.Moq.Config.Expectation == moq.Strict {
				m.Moq.Scene.T.Fatalf("Too many calls to %s", m.Moq.PrettyParams_UnreadByte(params))
			}
			return
		}
		i = results.Repeat.ResultCount - 1
	}

	result := results.Results[i]
	if result.Sequence != 0 {
		sequence := m.Moq.Scene.NextMockSequence()
		if (!results.Repeat.AnyTimes && result.Sequence != sequence) || result.Sequence > sequence {
			m.Moq.Scene.T.Fatalf("Call sequence does not match call to %s", m.Moq.PrettyParams_UnreadByte(params))
		}
	}

	if result.DoFn != nil {
		result.DoFn()
	}

	if result.Values != nil {
		result1 = result.Values.Result1
	}
	if result.DoReturnFn != nil {
		result1 = result.DoReturnFn()
	}
	return
}

// OnCall returns the recorder implementation of the ByteScanner type
func (m *MoqByteScanner) OnCall() *MoqByteScanner_recorder {
	return &MoqByteScanner_recorder{
		Moq: m,
	}
}

func (m *MoqByteScanner_recorder) ReadByte() *MoqByteScanner_ReadByte_fnRecorder {
	return &MoqByteScanner_ReadByte_fnRecorder{
		Params:   MoqByteScanner_ReadByte_params{},
		Sequence: m.Moq.Config.Sequence == moq.SeqDefaultOn,
		Moq:      m.Moq,
	}
}

func (r *MoqByteScanner_ReadByte_fnRecorder) Any() *MoqByteScanner_ReadByte_anyParams {
	r.Moq.Scene.T.Helper()
	if r.Results != nil {
		r.Moq.Scene.T.Fatalf("Any functions must be called before ReturnResults or DoReturnResults calls, recording %s", r.Moq.PrettyParams_ReadByte(r.Params))
		return nil
	}
	return &MoqByteScanner_ReadByte_anyParams{Recorder: r}
}

func (r *MoqByteScanner_ReadByte_fnRecorder) Seq() *MoqByteScanner_ReadByte_fnRecorder {
	r.Moq.Scene.T.Helper()
	if r.Results != nil {
		r.Moq.Scene.T.Fatalf("Seq must be called before ReturnResults or DoReturnResults calls, recording %s", r.Moq.PrettyParams_ReadByte(r.Params))
		return nil
	}
	r.Sequence = true
	return r
}

func (r *MoqByteScanner_ReadByte_fnRecorder) NoSeq() *MoqByteScanner_ReadByte_fnRecorder {
	r.Moq.Scene.T.Helper()
	if r.Results != nil {
		r.Moq.Scene.T.Fatalf("NoSeq must be called before ReturnResults or DoReturnResults calls, recording %s", r.Moq.PrettyParams_ReadByte(r.Params))
		return nil
	}
	r.Sequence = false
	return r
}

func (r *MoqByteScanner_ReadByte_fnRecorder) ReturnResults(result1 byte, result2 error) *MoqByteScanner_ReadByte_fnRecorder {
	r.Moq.Scene.T.Helper()
	r.FindResults()

	var sequence uint32
	if r.Sequence {
		sequence = r.Moq.Scene.NextRecorderSequence()
	}

	r.Results.Results = append(r.Results.Results, struct {
		Values *struct {
			Result1 byte
			Result2 error
		}
		Sequence   uint32
		DoFn       MoqByteScanner_ReadByte_doFn
		DoReturnFn MoqByteScanner_ReadByte_doReturnFn
	}{
		Values: &struct {
			Result1 byte
			Result2 error
		}{
			Result1: result1,
			Result2: result2,
		},
		Sequence: sequence,
	})
	return r
}

func (r *MoqByteScanner_ReadByte_fnRecorder) AndDo(fn MoqByteScanner_ReadByte_doFn) *MoqByteScanner_ReadByte_fnRecorder {
	r.Moq.Scene.T.Helper()
	if r.Results == nil {
		r.Moq.Scene.T.Fatalf("ReturnResults must be called before calling AndDo")
		return nil
	}
	last := &r.Results.Results[len(r.Results.Results)-1]
	last.DoFn = fn
	return r
}

func (r *MoqByteScanner_ReadByte_fnRecorder) DoReturnResults(fn MoqByteScanner_ReadByte_doReturnFn) *MoqByteScanner_ReadByte_fnRecorder {
	r.Moq.Scene.T.Helper()
	r.FindResults()

	var sequence uint32
	if r.Sequence {
		sequence = r.Moq.Scene.NextRecorderSequence()
	}

	r.Results.Results = append(r.Results.Results, struct {
		Values *struct {
			Result1 byte
			Result2 error
		}
		Sequence   uint32
		DoFn       MoqByteScanner_ReadByte_doFn
		DoReturnFn MoqByteScanner_ReadByte_doReturnFn
	}{Sequence: sequence, DoReturnFn: fn})
	return r
}

func (r *MoqByteScanner_ReadByte_fnRecorder) FindResults() {
	r.Moq.Scene.T.Helper()
	if r.Results != nil {
		r.Results.Repeat.Increment(r.Moq.Scene.T)
		return
	}

	anyCount := bits.OnesCount64(r.AnyParams)
	insertAt := -1
	var results *MoqByteScanner_ReadByte_resultsByParams
	for n, res := range r.Moq.ResultsByParams_ReadByte {
		if res.AnyParams == r.AnyParams {
			results = &res
			break
		}
		if res.AnyCount > anyCount {
			insertAt = n
		}
	}
	if results == nil {
		results = &MoqByteScanner_ReadByte_resultsByParams{
			AnyCount:  anyCount,
			AnyParams: r.AnyParams,
			Results:   map[MoqByteScanner_ReadByte_paramsKey]*MoqByteScanner_ReadByte_results{},
		}
		r.Moq.ResultsByParams_ReadByte = append(r.Moq.ResultsByParams_ReadByte, *results)
		if insertAt != -1 && insertAt+1 < len(r.Moq.ResultsByParams_ReadByte) {
			copy(r.Moq.ResultsByParams_ReadByte[insertAt+1:], r.Moq.ResultsByParams_ReadByte[insertAt:0])
			r.Moq.ResultsByParams_ReadByte[insertAt] = *results
		}
	}

	paramsKey := r.Moq.ParamsKey_ReadByte(r.Params, r.AnyParams)

	var ok bool
	r.Results, ok = results.Results[paramsKey]
	if !ok {
		r.Results = &MoqByteScanner_ReadByte_results{
			Params:  r.Params,
			Results: nil,
			Index:   0,
			Repeat:  &moq.RepeatVal{},
		}
		results.Results[paramsKey] = r.Results
	}

	r.Results.Repeat.Increment(r.Moq.Scene.T)
}

func (r *MoqByteScanner_ReadByte_fnRecorder) Repeat(repeaters ...moq.Repeater) *MoqByteScanner_ReadByte_fnRecorder {
	r.Moq.Scene.T.Helper()
	if r.Results == nil {
		r.Moq.Scene.T.Fatalf("ReturnResults or DoReturnResults must be called before calling Repeat")
		return nil
	}
	r.Results.Repeat.Repeat(r.Moq.Scene.T, repeaters)
	last := r.Results.Results[len(r.Results.Results)-1]
	for n := 0; n < r.Results.Repeat.ResultCount-1; n++ {
		if r.Sequence {
			last = struct {
				Values *struct {
					Result1 byte
					Result2 error
				}
				Sequence   uint32
				DoFn       MoqByteScanner_ReadByte_doFn
				DoReturnFn MoqByteScanner_ReadByte_doReturnFn
			}{
				Values:   last.Values,
				Sequence: r.Moq.Scene.NextRecorderSequence(),
			}
		}
		r.Results.Results = append(r.Results.Results, last)
	}
	return r
}

func (m *MoqByteScanner) PrettyParams_ReadByte(params MoqByteScanner_ReadByte_params) string {
	return fmt.Sprintf("ReadByte()")
}

func (m *MoqByteScanner) ParamsKey_ReadByte(params MoqByteScanner_ReadByte_params, anyParams uint64) MoqByteScanner_ReadByte_paramsKey {
	m.Scene.T.Helper()
	return MoqByteScanner_ReadByte_paramsKey{
		Params: struct{}{},
		Hashes: struct{}{},
	}
}

func (m *MoqByteScanner_recorder) UnreadByte() *MoqByteScanner_UnreadByte_fnRecorder {
	return &MoqByteScanner_UnreadByte_fnRecorder{
		Params:   MoqByteScanner_UnreadByte_params{},
		Sequence: m.Moq.Config.Sequence == moq.SeqDefaultOn,
		Moq:      m.Moq,
	}
}

func (r *MoqByteScanner_UnreadByte_fnRecorder) Any() *MoqByteScanner_UnreadByte_anyParams {
	r.Moq.Scene.T.Helper()
	if r.Results != nil {
		r.Moq.Scene.T.Fatalf("Any functions must be called before ReturnResults or DoReturnResults calls, recording %s", r.Moq.PrettyParams_UnreadByte(r.Params))
		return nil
	}
	return &MoqByteScanner_UnreadByte_anyParams{Recorder: r}
}

func (r *MoqByteScanner_UnreadByte_fnRecorder) Seq() *MoqByteScanner_UnreadByte_fnRecorder {
	r.Moq.Scene.T.Helper()
	if r.Results != nil {
		r.Moq.Scene.T.Fatalf("Seq must be called before ReturnResults or DoReturnResults calls, recording %s", r.Moq.PrettyParams_UnreadByte(r.Params))
		return nil
	}
	r.Sequence = true
	return r
}

func (r *MoqByteScanner_UnreadByte_fnRecorder) NoSeq() *MoqByteScanner_UnreadByte_fnRecorder {
	r.Moq.Scene.T.Helper()
	if r.Results != nil {
		r.Moq.Scene.T.Fatalf("NoSeq must be called before ReturnResults or DoReturnResults calls, recording %s", r.Moq.PrettyParams_UnreadByte(r.Params))
		return nil
	}
	r.Sequence = false
	return r
}

func (r *MoqByteScanner_UnreadByte_fnRecorder) ReturnResults(result1 error) *MoqByteScanner_UnreadByte_fnRecorder {
	r.Moq.Scene.T.Helper()
	r.FindResults()

	var sequence uint32
	if r.Sequence {
		sequence = r.Moq.Scene.NextRecorderSequence()
	}

	r.Results.Results = append(r.Results.Results, struct {
		Values *struct {
			Result1 error
		}
		Sequence   uint32
		DoFn       MoqByteScanner_UnreadByte_doFn
		DoReturnFn MoqByteScanner_UnreadByte_doReturnFn
	}{
		Values: &struct {
			Result1 error
		}{
			Result1: result1,
		},
		Sequence: sequence,
	})
	return r
}

func (r *MoqByteScanner_UnreadByte_fnRecorder) AndDo(fn MoqByteScanner_UnreadByte_doFn) *MoqByteScanner_UnreadByte_fnRecorder {
	r.Moq.Scene.T.Helper()
	if r.Results == nil {
		r.Moq.Scene.T.Fatalf("ReturnResults must be called before calling AndDo")
		return nil
	}
	last := &r.Results.Results[len(r.Results.Results)-1]
	last.DoFn = fn
	return r
}

func (r *MoqByteScanner_UnreadByte_fnRecorder) DoReturnResults(fn MoqByteScanner_UnreadByte_doReturnFn) *MoqByteScanner_UnreadByte_fnRecorder {
	r.Moq.Scene.T.Helper()
	r.FindResults()

	var sequence uint32
	if r.Sequence {
		sequence = r.Moq.Scene.NextRecorderSequence()
	}

	r.Results.Results = append(r.Results.Results, struct {
		Values *struct {
			Result1 error
		}
		Sequence   uint32
		DoFn       MoqByteScanner_UnreadByte_doFn
		DoReturnFn MoqByteScanner_UnreadByte_doReturnFn
	}{Sequence: sequence, DoReturnFn: fn})
	return r
}

func (r *MoqByteScanner_UnreadByte_fnRecorder) FindResults() {
	r.Moq.Scene.T.Helper()
	if r.Results != nil {
		r.Results.Repeat.Increment(r.Moq.Scene.T)
		return
	}

	anyCount := bits.OnesCount64(r.AnyParams)
	insertAt := -1
	var results *MoqByteScanner_UnreadByte_resultsByParams
	for n, res := range r.Moq.ResultsByParams_UnreadByte {
		if res.AnyParams == r.AnyParams {
			results = &res
			break
		}
		if res.AnyCount > anyCount {
			insertAt = n
		}
	}
	if results == nil {
		results = &MoqByteScanner_UnreadByte_resultsByParams{
			AnyCount:  anyCount,
			AnyParams: r.AnyParams,
			Results:   map[MoqByteScanner_UnreadByte_paramsKey]*MoqByteScanner_UnreadByte_results{},
		}
		r.Moq.ResultsByParams_UnreadByte = append(r.Moq.ResultsByParams_UnreadByte, *results)
		if insertAt != -1 && insertAt+1 < len(r.Moq.ResultsByParams_UnreadByte) {
			copy(r.Moq.ResultsByParams_UnreadByte[insertAt+1:], r.Moq.ResultsByParams_UnreadByte[insertAt:0])
			r.Moq.ResultsByParams_UnreadByte[insertAt] = *results
		}
	}

	paramsKey := r.Moq.ParamsKey_UnreadByte(r.Params, r.AnyParams)

	var ok bool
	r.Results, ok = results.Results[paramsKey]
	if !ok {
		r.Results = &MoqByteScanner_UnreadByte_results{
			Params:  r.Params,
			Results: nil,
			Index:   0,
			Repeat:  &moq.RepeatVal{},
		}
		results.Results[paramsKey] = r.Results
	}

	r.Results.Repeat.Increment(r.Moq.Scene.T)
}

func (r *MoqByteScanner_UnreadByte_fnRecorder) Repeat(repeaters ...moq.Repeater) *MoqByteScanner_UnreadByte_fnRecorder {
	r.Moq.Scene.T.Helper()
	if r.Results == nil {
		r.Moq.Scene.T.Fatalf("ReturnResults or DoReturnResults must be called before calling Repeat")
		return nil
	}
	r.Results.Repeat.Repeat(r.Moq.Scene.T, repeaters)
	last := r.Results.Results[len(r.Results.Results)-1]
	for n := 0; n < r.Results.Repeat.ResultCount-1; n++ {
		if r.Sequence {
			last = struct {
				Values *struct {
					Result1 error
				}
				Sequence   uint32
				DoFn       MoqByteScanner_UnreadByte_doFn
				DoReturnFn MoqByteScanner_UnreadByte_doReturnFn
			}{
				Values:   last.Values,
				Sequence: r.Moq.Scene.NextRecorderSequence(),
			}
		}
		r.Results.Results = append(r.Results.Results, last)
	}
	return r
}

func (m *MoqByteScanner) PrettyParams_UnreadByte(params MoqByteScanner_UnreadByte_params) string {
	return fmt.Sprintf("UnreadByte()")
}

func (m *MoqByteScanner) ParamsKey_UnreadByte(params MoqByteScanner_UnreadByte_params, anyParams uint64) MoqByteScanner_UnreadByte_paramsKey {
	m.Scene.T.Helper()
	return MoqByteScanner_UnreadByte_paramsKey{
		Params: struct{}{},
		Hashes: struct{}{},
	}
}

// Reset resets the state of the moq
func (m *MoqByteScanner) Reset() {
	m.ResultsByParams_ReadByte = nil
	m.ResultsByParams_UnreadByte = nil
}

// AssertExpectationsMet asserts that all expectations have been met
func (m *MoqByteScanner) AssertExpectationsMet() {
	m.Scene.T.Helper()
	for _, res := range m.ResultsByParams_ReadByte {
		for _, results := range res.Results {
			missing := results.Repeat.MinTimes - int(atomic.LoadUint32(&results.Index))
			if missing > 0 {
				m.Scene.T.Errorf("Expected %d additional call(s) to %s", missing, m.PrettyParams_ReadByte(results.Params))
			}
		}
	}
	for _, res := range m.ResultsByParams_UnreadByte {
		for _, results := range res.Results {
			missing := results.Repeat.MinTimes - int(atomic.LoadUint32(&results.Index))
			if missing > 0 {
				m.Scene.T.Errorf("Expected %d additional call(s) to %s", missing, m.PrettyParams_UnreadByte(results.Params))
			}
		}
	}
}
