// Code generated by Moqueries - https://moqueries.org - DO NOT EDIT!

package syscall

import (
	"fmt"
	"math/bits"
	"sync/atomic"

	"moqueries.org/runtime/hash"
	"moqueries.org/runtime/moq"
)

// InotifyAddWatch_genType is the fabricated implementation type of this mock
// (emitted when mocking functions directly and not from a function type)
type InotifyAddWatch_genType func(fd int, pathname string, mask uint32) (watchdesc int, err error)

// MoqInotifyAddWatch_genType holds the state of a moq of the
// InotifyAddWatch_genType type
type MoqInotifyAddWatch_genType struct {
	Scene  *moq.Scene
	Config moq.Config
	Moq    *MoqInotifyAddWatch_genType_mock

	ResultsByParams []MoqInotifyAddWatch_genType_resultsByParams

	Runtime struct {
		ParameterIndexing struct {
			Fd       moq.ParamIndexing
			Pathname moq.ParamIndexing
			Mask     moq.ParamIndexing
		}
	}
}

// MoqInotifyAddWatch_genType_mock isolates the mock interface of the
// InotifyAddWatch_genType type
type MoqInotifyAddWatch_genType_mock struct {
	Moq *MoqInotifyAddWatch_genType
}

// MoqInotifyAddWatch_genType_params holds the params of the
// InotifyAddWatch_genType type
type MoqInotifyAddWatch_genType_params struct {
	Fd       int
	Pathname string
	Mask     uint32
}

// MoqInotifyAddWatch_genType_paramsKey holds the map key params of the
// InotifyAddWatch_genType type
type MoqInotifyAddWatch_genType_paramsKey struct {
	Params struct {
		Fd       int
		Pathname string
		Mask     uint32
	}
	Hashes struct {
		Fd       hash.Hash
		Pathname hash.Hash
		Mask     hash.Hash
	}
}

// MoqInotifyAddWatch_genType_resultsByParams contains the results for a given
// set of parameters for the InotifyAddWatch_genType type
type MoqInotifyAddWatch_genType_resultsByParams struct {
	AnyCount  int
	AnyParams uint64
	Results   map[MoqInotifyAddWatch_genType_paramsKey]*MoqInotifyAddWatch_genType_results
}

// MoqInotifyAddWatch_genType_doFn defines the type of function needed when
// calling AndDo for the InotifyAddWatch_genType type
type MoqInotifyAddWatch_genType_doFn func(fd int, pathname string, mask uint32)

// MoqInotifyAddWatch_genType_doReturnFn defines the type of function needed
// when calling DoReturnResults for the InotifyAddWatch_genType type
type MoqInotifyAddWatch_genType_doReturnFn func(fd int, pathname string, mask uint32) (watchdesc int, err error)

// MoqInotifyAddWatch_genType_results holds the results of the
// InotifyAddWatch_genType type
type MoqInotifyAddWatch_genType_results struct {
	Params  MoqInotifyAddWatch_genType_params
	Results []struct {
		Values *struct {
			Watchdesc int
			Err       error
		}
		Sequence   uint32
		DoFn       MoqInotifyAddWatch_genType_doFn
		DoReturnFn MoqInotifyAddWatch_genType_doReturnFn
	}
	Index  uint32
	Repeat *moq.RepeatVal
}

// MoqInotifyAddWatch_genType_fnRecorder routes recorded function calls to the
// MoqInotifyAddWatch_genType moq
type MoqInotifyAddWatch_genType_fnRecorder struct {
	Params    MoqInotifyAddWatch_genType_params
	AnyParams uint64
	Sequence  bool
	Results   *MoqInotifyAddWatch_genType_results
	Moq       *MoqInotifyAddWatch_genType
}

// MoqInotifyAddWatch_genType_anyParams isolates the any params functions of
// the InotifyAddWatch_genType type
type MoqInotifyAddWatch_genType_anyParams struct {
	Recorder *MoqInotifyAddWatch_genType_fnRecorder
}

// NewMoqInotifyAddWatch_genType creates a new moq of the
// InotifyAddWatch_genType type
func NewMoqInotifyAddWatch_genType(scene *moq.Scene, config *moq.Config) *MoqInotifyAddWatch_genType {
	if config == nil {
		config = &moq.Config{}
	}
	m := &MoqInotifyAddWatch_genType{
		Scene:  scene,
		Config: *config,
		Moq:    &MoqInotifyAddWatch_genType_mock{},

		Runtime: struct {
			ParameterIndexing struct {
				Fd       moq.ParamIndexing
				Pathname moq.ParamIndexing
				Mask     moq.ParamIndexing
			}
		}{ParameterIndexing: struct {
			Fd       moq.ParamIndexing
			Pathname moq.ParamIndexing
			Mask     moq.ParamIndexing
		}{
			Fd:       moq.ParamIndexByValue,
			Pathname: moq.ParamIndexByValue,
			Mask:     moq.ParamIndexByValue,
		}},
	}
	m.Moq.Moq = m

	scene.AddMoq(m)
	return m
}

// Mock returns the moq implementation of the InotifyAddWatch_genType type
func (m *MoqInotifyAddWatch_genType) Mock() InotifyAddWatch_genType {
	return func(fd int, pathname string, mask uint32) (_ int, _ error) {
		m.Scene.T.Helper()
		moq := &MoqInotifyAddWatch_genType_mock{Moq: m}
		return moq.Fn(fd, pathname, mask)
	}
}

func (m *MoqInotifyAddWatch_genType_mock) Fn(fd int, pathname string, mask uint32) (watchdesc int, err error) {
	m.Moq.Scene.T.Helper()
	params := MoqInotifyAddWatch_genType_params{
		Fd:       fd,
		Pathname: pathname,
		Mask:     mask,
	}
	var results *MoqInotifyAddWatch_genType_results
	for _, resultsByParams := range m.Moq.ResultsByParams {
		paramsKey := m.Moq.ParamsKey(params, resultsByParams.AnyParams)
		var ok bool
		results, ok = resultsByParams.Results[paramsKey]
		if ok {
			break
		}
	}
	if results == nil {
		if m.Moq.Config.Expectation == moq.Strict {
			m.Moq.Scene.T.Fatalf("Unexpected call to %s", m.Moq.PrettyParams(params))
		}
		return
	}

	i := int(atomic.AddUint32(&results.Index, 1)) - 1
	if i >= results.Repeat.ResultCount {
		if !results.Repeat.AnyTimes {
			if m.Moq.Config.Expectation == moq.Strict {
				m.Moq.Scene.T.Fatalf("Too many calls to %s", m.Moq.PrettyParams(params))
			}
			return
		}
		i = results.Repeat.ResultCount - 1
	}

	result := results.Results[i]
	if result.Sequence != 0 {
		sequence := m.Moq.Scene.NextMockSequence()
		if (!results.Repeat.AnyTimes && result.Sequence != sequence) || result.Sequence > sequence {
			m.Moq.Scene.T.Fatalf("Call sequence does not match call to %s", m.Moq.PrettyParams(params))
		}
	}

	if result.DoFn != nil {
		result.DoFn(fd, pathname, mask)
	}

	if result.Values != nil {
		watchdesc = result.Values.Watchdesc
		err = result.Values.Err
	}
	if result.DoReturnFn != nil {
		watchdesc, err = result.DoReturnFn(fd, pathname, mask)
	}
	return
}

func (m *MoqInotifyAddWatch_genType) OnCall(fd int, pathname string, mask uint32) *MoqInotifyAddWatch_genType_fnRecorder {
	return &MoqInotifyAddWatch_genType_fnRecorder{
		Params: MoqInotifyAddWatch_genType_params{
			Fd:       fd,
			Pathname: pathname,
			Mask:     mask,
		},
		Sequence: m.Config.Sequence == moq.SeqDefaultOn,
		Moq:      m,
	}
}

func (r *MoqInotifyAddWatch_genType_fnRecorder) Any() *MoqInotifyAddWatch_genType_anyParams {
	r.Moq.Scene.T.Helper()
	if r.Results != nil {
		r.Moq.Scene.T.Fatalf("Any functions must be called before ReturnResults or DoReturnResults calls, recording %s", r.Moq.PrettyParams(r.Params))
		return nil
	}
	return &MoqInotifyAddWatch_genType_anyParams{Recorder: r}
}

func (a *MoqInotifyAddWatch_genType_anyParams) Fd() *MoqInotifyAddWatch_genType_fnRecorder {
	a.Recorder.AnyParams |= 1 << 0
	return a.Recorder
}

func (a *MoqInotifyAddWatch_genType_anyParams) Pathname() *MoqInotifyAddWatch_genType_fnRecorder {
	a.Recorder.AnyParams |= 1 << 1
	return a.Recorder
}

func (a *MoqInotifyAddWatch_genType_anyParams) Mask() *MoqInotifyAddWatch_genType_fnRecorder {
	a.Recorder.AnyParams |= 1 << 2
	return a.Recorder
}

func (r *MoqInotifyAddWatch_genType_fnRecorder) Seq() *MoqInotifyAddWatch_genType_fnRecorder {
	r.Moq.Scene.T.Helper()
	if r.Results != nil {
		r.Moq.Scene.T.Fatalf("Seq must be called before ReturnResults or DoReturnResults calls, recording %s", r.Moq.PrettyParams(r.Params))
		return nil
	}
	r.Sequence = true
	return r
}

func (r *MoqInotifyAddWatch_genType_fnRecorder) NoSeq() *MoqInotifyAddWatch_genType_fnRecorder {
	r.Moq.Scene.T.Helper()
	if r.Results != nil {
		r.Moq.Scene.T.Fatalf("NoSeq must be called before ReturnResults or DoReturnResults calls, recording %s", r.Moq.PrettyParams(r.Params))
		return nil
	}
	r.Sequence = false
	return r
}

func (r *MoqInotifyAddWatch_genType_fnRecorder) ReturnResults(watchdesc int, err error) *MoqInotifyAddWatch_genType_fnRecorder {
	r.Moq.Scene.T.Helper()
	r.FindResults()

	var sequence uint32
	if r.Sequence {
		sequence = r.Moq.Scene.NextRecorderSequence()
	}

	r.Results.Results = append(r.Results.Results, struct {
		Values *struct {
			Watchdesc int
			Err       error
		}
		Sequence   uint32
		DoFn       MoqInotifyAddWatch_genType_doFn
		DoReturnFn MoqInotifyAddWatch_genType_doReturnFn
	}{
		Values: &struct {
			Watchdesc int
			Err       error
		}{
			Watchdesc: watchdesc,
			Err:       err,
		},
		Sequence: sequence,
	})
	return r
}

func (r *MoqInotifyAddWatch_genType_fnRecorder) AndDo(fn MoqInotifyAddWatch_genType_doFn) *MoqInotifyAddWatch_genType_fnRecorder {
	r.Moq.Scene.T.Helper()
	if r.Results == nil {
		r.Moq.Scene.T.Fatalf("ReturnResults must be called before calling AndDo")
		return nil
	}
	last := &r.Results.Results[len(r.Results.Results)-1]
	last.DoFn = fn
	return r
}

func (r *MoqInotifyAddWatch_genType_fnRecorder) DoReturnResults(fn MoqInotifyAddWatch_genType_doReturnFn) *MoqInotifyAddWatch_genType_fnRecorder {
	r.Moq.Scene.T.Helper()
	r.FindResults()

	var sequence uint32
	if r.Sequence {
		sequence = r.Moq.Scene.NextRecorderSequence()
	}

	r.Results.Results = append(r.Results.Results, struct {
		Values *struct {
			Watchdesc int
			Err       error
		}
		Sequence   uint32
		DoFn       MoqInotifyAddWatch_genType_doFn
		DoReturnFn MoqInotifyAddWatch_genType_doReturnFn
	}{Sequence: sequence, DoReturnFn: fn})
	return r
}

func (r *MoqInotifyAddWatch_genType_fnRecorder) FindResults() {
	r.Moq.Scene.T.Helper()
	if r.Results != nil {
		r.Results.Repeat.Increment(r.Moq.Scene.T)
		return
	}

	anyCount := bits.OnesCount64(r.AnyParams)
	insertAt := -1
	var results *MoqInotifyAddWatch_genType_resultsByParams
	for n, res := range r.Moq.ResultsByParams {
		if res.AnyParams == r.AnyParams {
			results = &res
			break
		}
		if res.AnyCount > anyCount {
			insertAt = n
		}
	}
	if results == nil {
		results = &MoqInotifyAddWatch_genType_resultsByParams{
			AnyCount:  anyCount,
			AnyParams: r.AnyParams,
			Results:   map[MoqInotifyAddWatch_genType_paramsKey]*MoqInotifyAddWatch_genType_results{},
		}
		r.Moq.ResultsByParams = append(r.Moq.ResultsByParams, *results)
		if insertAt != -1 && insertAt+1 < len(r.Moq.ResultsByParams) {
			copy(r.Moq.ResultsByParams[insertAt+1:], r.Moq.ResultsByParams[insertAt:0])
			r.Moq.ResultsByParams[insertAt] = *results
		}
	}

	paramsKey := r.Moq.ParamsKey(r.Params, r.AnyParams)

	var ok bool
	r.Results, ok = results.Results[paramsKey]
	if !ok {
		r.Results = &MoqInotifyAddWatch_genType_results{
			Params:  r.Params,
			Results: nil,
			Index:   0,
			Repeat:  &moq.RepeatVal{},
		}
		results.Results[paramsKey] = r.Results
	}

	r.Results.Repeat.Increment(r.Moq.Scene.T)
}

func (r *MoqInotifyAddWatch_genType_fnRecorder) Repeat(repeaters ...moq.Repeater) *MoqInotifyAddWatch_genType_fnRecorder {
	r.Moq.Scene.T.Helper()
	if r.Results == nil {
		r.Moq.Scene.T.Fatalf("ReturnResults or DoReturnResults must be called before calling Repeat")
		return nil
	}
	r.Results.Repeat.Repeat(r.Moq.Scene.T, repeaters)
	last := r.Results.Results[len(r.Results.Results)-1]
	for n := 0; n < r.Results.Repeat.ResultCount-1; n++ {
		if r.Sequence {
			last = struct {
				Values *struct {
					Watchdesc int
					Err       error
				}
				Sequence   uint32
				DoFn       MoqInotifyAddWatch_genType_doFn
				DoReturnFn MoqInotifyAddWatch_genType_doReturnFn
			}{
				Values:   last.Values,
				Sequence: r.Moq.Scene.NextRecorderSequence(),
			}
		}
		r.Results.Results = append(r.Results.Results, last)
	}
	return r
}

func (m *MoqInotifyAddWatch_genType) PrettyParams(params MoqInotifyAddWatch_genType_params) string {
	return fmt.Sprintf("InotifyAddWatch_genType(%#v, %#v, %#v)", params.Fd, params.Pathname, params.Mask)
}

func (m *MoqInotifyAddWatch_genType) ParamsKey(params MoqInotifyAddWatch_genType_params, anyParams uint64) MoqInotifyAddWatch_genType_paramsKey {
	m.Scene.T.Helper()
	var fdUsed int
	var fdUsedHash hash.Hash
	if anyParams&(1<<0) == 0 {
		if m.Runtime.ParameterIndexing.Fd == moq.ParamIndexByValue {
			fdUsed = params.Fd
		} else {
			fdUsedHash = hash.DeepHash(params.Fd)
		}
	}
	var pathnameUsed string
	var pathnameUsedHash hash.Hash
	if anyParams&(1<<1) == 0 {
		if m.Runtime.ParameterIndexing.Pathname == moq.ParamIndexByValue {
			pathnameUsed = params.Pathname
		} else {
			pathnameUsedHash = hash.DeepHash(params.Pathname)
		}
	}
	var maskUsed uint32
	var maskUsedHash hash.Hash
	if anyParams&(1<<2) == 0 {
		if m.Runtime.ParameterIndexing.Mask == moq.ParamIndexByValue {
			maskUsed = params.Mask
		} else {
			maskUsedHash = hash.DeepHash(params.Mask)
		}
	}
	return MoqInotifyAddWatch_genType_paramsKey{
		Params: struct {
			Fd       int
			Pathname string
			Mask     uint32
		}{
			Fd:       fdUsed,
			Pathname: pathnameUsed,
			Mask:     maskUsed,
		},
		Hashes: struct {
			Fd       hash.Hash
			Pathname hash.Hash
			Mask     hash.Hash
		}{
			Fd:       fdUsedHash,
			Pathname: pathnameUsedHash,
			Mask:     maskUsedHash,
		},
	}
}

// Reset resets the state of the moq
func (m *MoqInotifyAddWatch_genType) Reset() { m.ResultsByParams = nil }

// AssertExpectationsMet asserts that all expectations have been met
func (m *MoqInotifyAddWatch_genType) AssertExpectationsMet() {
	m.Scene.T.Helper()
	for _, res := range m.ResultsByParams {
		for _, results := range res.Results {
			missing := results.Repeat.MinTimes - int(atomic.LoadUint32(&results.Index))
			if missing > 0 {
				m.Scene.T.Errorf("Expected %d additional call(s) to %s", missing, m.PrettyParams(results.Params))
			}
		}
	}
}
